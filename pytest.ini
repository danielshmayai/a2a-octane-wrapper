[pytest]
# Only collect unit tests from the tests/ directory.
# test_a2a_auth_flow.py is a standalone integration script that requires
# live servers (A2A wrapper + Mock OAuth) and is NOT run by the pre-commit hook.
testpaths = tests
asyncio_mode = strict
